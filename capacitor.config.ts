import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'attendance-qr-scanner',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
